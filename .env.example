# Environment Variables for Vercel Deployment
# Add these in Vercel Dashboard → Settings → Environment Variables

# MongoDB Connection (required)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database

# JWT Secret (required) - Generate a random string
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters

# Vercel Blob Storage (required for images)
# Automatically set when you enable Vercel Blob in your project
BLOB_READ_WRITE_TOKEN=vercel_blob_xxx

# Default Admin Account (optional - auto-created on first request)
ADMIN_NAME=Administrator
ADMIN_EMAIL=admin@tecnoimagen.com
ADMIN_PASSWORD=YourSecurePassword123!

# Google AI API Key (optional - for AI features)
GOOGLE_API_KEY=your-google-ai-api-key

# Frontend API URL (leave empty for production)
# REACT_APP_API_URL=http://localhost:5000

